<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title></title>
</head>
<body>
    </br> </br>
    <h2>          Show Competance </h2>
    </br> </br>
    <div class="card">
        <center>
            <table class="table table-bordered table-sm" id="users">
                <tbody>
                    <tr>

                        <th>Name</th>
                        <th>Description</th>
                        <th>Favoris</th>
                        @*<th>Poste</th>
                        <th>Categorie</th>*@
                    </tr>
                    @foreach (var p in ViewBag.result)
                    {
                        <tr>

                            <td>@p.nom</td>
                            <td>@p.description</td>
                            <td>@p.isLiked</td>
                            @*<td>@p.poste.nom</td>
                            <td>@p.categorie.nom</td>*@
                            <td>

                                <a href="@Url.Action("Delete", "Competance", new { id = p.id })"><button class="mb-1 btn btn-danger" type="button">Delete</button></a>

                            </td>
                        </tr>
                    }
                </tbody>

            </table>

            </br>
            <a href="@Url.Action("Create","Competance")"> <button class="btn btn-primary" type="button">Add new competance</button> </a>
        </center>
    </div>
</body>
</html>