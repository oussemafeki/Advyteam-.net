@model Advyteam.Models.CompetanceModel

@{
    ViewBag.Title = "Delete";
}
<center>

    <h2>Delete</h2>

    <h3>Are you sure you want to delete this Skills ?</h3>
    <div>

        <hr />
        <dl class="dl-horizontal">
            <dt>
                <h6>Nom : </h6>
                <h3>@Html.DisplayFor(model => model.nom) </h3>
            </dt>

            <dd>

            </dd>
            <dt>
                <h6>Description : </h6>
                @Html.DisplayFor(model => model.description)
            </dt>

            <dd>

            </dd>
            @*<dt>
                <h6>Favoris : </h6>
                @Html.DisplayFor(model => model.isLiked)
            </dt>*@

            <dd>

            </dd>
            
        </dl>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-actions no-color">

                <a href="@Url.Action("Index","Competance")"> <button class="btn btn-primary" type="button">Back to list</button> </a>
                <input type="submit" value="Delete" class="btn btn-primary" /> |
            </div>
        }
    </div>
</center>